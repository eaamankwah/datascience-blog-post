# ============================================================
# requirements.txt
# All packages required for the Credit Card Fraud Detection
# project — covers data processing, modelling, resampling,
# explainability, visualisation, and the Streamlit dashboard.
# Install: pip install -r requirements.txt
# ============================================================

# ── Core data handling ────────────────────────────────────────
pandas>=2.0.0
numpy>=1.25.0

# ── Machine learning ──────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.0

# ── Class imbalance handling ──────────────────────────────────
imbalanced-learn>=0.12.0   # Provides SMOTE and RandomUnderSampler

# ── Explainability ────────────────────────────────────────────
shap>=0.45.0

# ── Visualisation ─────────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.13.0

# ── Interactive dashboard ─────────────────────────────────────
streamlit>=1.35.0

# ── Notebook support (optional, for .ipynb development) ───────
jupyter>=1.0.0
ipykernel>=6.29.0
ipywidgets>=8.1.0           # Needed for SHAP plots inside Jupyter

# ── Utilities ─────────────────────────────────────────────────
scipy>=1.11.0               # Required by imbalanced-learn internals
joblib>=1.3.0               # Model serialisation and caching
tqdm>=4.66.0                # Progress bars during long training runs

# ── Fairness & bias framework ────────────────────────────────────────
aequitas=1.1.0	            # Fairness & bias auditing

# ── Optional: model saving & loading ─────────────────────────
# Uncomment if you want to persist trained models to disk
# pickle5>=0.0.12           # Enhanced pickle for large model objects